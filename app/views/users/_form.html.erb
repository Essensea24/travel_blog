<%= form_for(@user) do |f| %>

<!-- setting condition to display errors if any save/create a new article if there is an error -->
 
	<% if @user.errors.any? %>
		<div id="error_explanation">
			<h2>
				<%= pluralize(@user.errors.count, "error") %> prohibited
				this user from being saved:
			</h2>
			<ul>
				<% @user.errors.full_messages.each do |msg| %>
				  <li><%= msg %></li>
				<% end %>
			</ul>
		</div>
	<% end %>
 
	<p>
		<%= f.label :first_name %><br>
		<%= f.text_field :first_name %>
	</p>

	<p>
		<%= f.label :last_name %><br>
		<%= f.text_area :last_name %>
	</p>

	<p>
		<%= f.label :username %><br>
		<%= f.text_area :username %>
	</p>

	<p>
		<%= f.label :password %><br>
		<%= f.text_area :password %>
	</p>

	<p>
		<%= f.label :blog_page %><br>
		<%= f.text_area :blog_page %>
	</p>

	<p>
		<%= f.label :profile_pic %><br>
		<%= f.text_area :profile_pic %>
	</p>
	<p>
	<%= f.submit %>
	</p>

<% end %>